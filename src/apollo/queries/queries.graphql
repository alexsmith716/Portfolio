#import "./fragmentCharactersResults.graphql"

query HelloWorld {
	helloWorld
}

query GetAllRickAndMortyCharacters($page: Int, $filter: FilterCharacter){
	characters(page: $page, filter: $filter) {
		info {
			next
			prev
			pages
			count
		}
		results {
			name
			image
		}
	}
}
